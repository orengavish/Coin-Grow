# Coin Grow — Level Builder Specification
### For: Junior Developer Onboarding
### Project: Coin Grow (AI-generated financial literacy game)
### Status: Pre-funding, two co-founders

---

## 1. What This Document Is

Your primary mission is to build one thing: **a Level Builder Machine** — an AI-powered pipeline that takes a text description of a game level and outputs a fully playable, animated 2D game scene.

This document covers:
- What the game is and why this matters
- What the level builder must produce
- Architecture decisions already made
- What needs to be built
- Key open questions

---

## 2. The Game — Coin Grow

**Core idea:** An AI-generated financial literacy game for kids (8+) through adults. Players learn money concepts by *living* them — bartering in a medieval market, choosing how to spend, discovering consequences.

**The prompt is the product.** The AI prompt engine generates all lesson content: story, characters, branching paths, outcomes, debriefs. The game engine is just a delivery vehicle for what the AI writes.

**Social layer:** Classrooms compete against each other. Schools against schools. A market simulator for investment competitions is the future tier.

**Current milestone:** A working Lesson 1 — "The Orange Quest" — where a 12-year-old named Eli must barter for an orange to save his sick little brother Luca before dawn.

---

## 3. What a "Level" Is

A lesson in Coin Grow has this structure:

```
Cinematic Intro        → Animated story scene, sets emotional stakes
    ↓
Intro Scenes           → Prof Penny (owl mentor) introduces the concept
    ↓
Path Selection         → Player chooses how to solve the problem (3-5 options)
    ↓
    ├── Dead End Paths → Teach a micro-lesson, return to selection
    └── Live Paths     → Play through scenes, earn points
    ↓
Ending Scenes          → Resolution, consequence plays out
    ↓
Debrief                → Prof Penny explains the concept, trophy awarded
```

Each path is a mini adventure with its own scenes, NPCs, and dialogue.

The **lesson JSON** (generated by Claude AI) defines all of this. The game engine reads it and renders it. The JSON schema includes:
- `intro_scenes`, `paths`, `ending_scenes`
- Characters with bilingual dialogue (`en`/`it`)
- `scoring.path_points`, `debrief`, `trophy`

---

## 4. Architecture Decisions (Already Made)

| Layer | Decision | Why |
|---|---|---|
| Mobile framework | **Flutter** (Dart) | Cross-platform iOS/Android, one codebase |
| Game engine | **Flame** (Flutter plugin) | 2D game engine inside Flutter — keeps lesson UI and game world in one codebase |
| Character animation | **Rive** | Skeletal animation, state machines, small file size. Used by Duolingo. |
| Lesson content | **Claude AI** (Anthropic API) | Prompt-generated JSON lessons |
| Backgrounds | **AI-generated images** (Bing/DALL-E) | Free, fast, cartoon storybook style |
| Character sprites | **Kenney.nl** free packs (CC0) | Professional quality, pre-cut walk cycles, no cost |
| Mentor character | **Prof Penny** (owl) — 3 PNG poses exist: wave, thoughtful, celebrate | Will become Rive rig |
| State management | **Provider** | Simple, fits the lesson state machine |
| Navigation | **Go Router** | Standard Flutter routing |
| Backend | None yet — local JSON assets | API connection planned for later |

---

## 5. Current State of the Codebase

### Repository: `github.com/orengavish/Coin-Grow`

```
prompts/                        ← AI prompt files (core IP — never overwrite)
lessons/                        ← Generated lesson JSON (git-ignored)
app.py                          ← Streamlit Studio (prompt iteration UI)
test_harness.py                 ← CLI lesson generator
curriculum.json                 ← Lesson ordering
mobile/
  lib/
    main.dart                   ← App entry point
    models/lesson.dart          ← Dart models for lesson JSON schema
    screens/
      home_screen.dart          ← Lesson picker
      cinematic_intro_screen.dart  ← Animated story opening (YOUR TARGET)
      lesson_player.dart        ← Zone state machine (intro→paths→ending)
      path_selection_screen.dart
      dead_end_screen.dart
    game/scenes/                ← Scene renderers (cutscene, dialogue, debrief)
    widgets/                    ← NarrativeBox, CharacterPortrait
    theme/app_theme.dart        ← Colors, styles
  assets/
    images/                     ← Prof Penny PNGs, scene background images
    lessons/                    ← Lesson 1 JSON (offline/demo)
```

### What works today
- Full lesson flow: cinematic intro → path selection → dead ends → ending → debrief
- Prof Penny PNG portraits with bobbing animation
- Scene transitions with fade
- Dialogue screens with typewriter + fade between lines
- Cinematic intro: AI-generated background image + Ken Burns zoom + speech bubble
- Windows Desktop build working (dev testing)

### What is missing / your job
- **Flame game world** — none of this is built yet
- **Walking characters** on a market path
- **Interaction zones** — player approaches vendor, triggers dialogue
- **Animated sprites** — walk cycles, idle, react
- **Prof Penny Rive rig** — she needs to actually move
- **Level builder pipeline** — see section 6

---

## 6. The Level Builder — Your Mission

**Goal:** Given a text description of a game scene, produce a fully playable animated 2D Flame level.

### What "a level" means in Flame terms

```
FlameGame
  └── World
        ├── Background layer      (parallax scrolling AI image)
        ├── Path layer            (bezier/waypoint path the player walks)
        ├── NPC sprites           (at stalls, placed along path)
        ├── Player sprite         (Eli, walks the path, stops at NPCs)
        ├── Interaction zones     (proximity triggers dialogue)
        └── Flutter overlay       (dialogue card, HUD, NarrativeBox)
```

### What the Level Builder Pipeline must do

```
INPUT:  Text description
        e.g. "A medieval market street. Eli walks left to right.
               Three vendor stalls: fisherman, bread baker, orange seller.
               Orange seller is at the far right."

    ↓ Step 1: AI Layout Generator
        Call Claude API with description
        Output: JSON scene layout
        {
          "background": "medieval_market_day",
          "path": [[0.0, 0.6], [0.3, 0.58], [0.7, 0.62], [1.0, 0.6]],
          "npcs": [
            {"id": "fisherman", "x": 0.25, "sprite": "vendor_male_1", "dialogue_ref": "path_fish"},
            {"id": "baker",     "x": 0.55, "sprite": "vendor_female_1", "dialogue_ref": "path_bread"},
            {"id": "orange_seller", "x": 0.85, "sprite": "vendor_male_2", "dialogue_ref": "path_orange"}
          ],
          "player_start": [0.0, 0.6],
          "interaction_radius": 0.08
        }

    ↓ Step 2: Asset Resolver
        Map sprite IDs → actual PNG/sprite sheet files from Kenney pack
        Map background → AI-generated image (or generate one via API)

    ↓ Step 3: Flame Level Builder
        Read the layout JSON
        Instantiate Flame components:
          - ParallaxComponent for background
          - SpriteAnimationComponent for each NPC (idle loop)
          - SpriteAnimationComponent for player (walk along path)
          - CircleHitbox interaction zones
          - Flutter overlay trigger on zone enter

OUTPUT: Playable Flame scene, integrated with existing lesson_player.dart
```

### What you are NOT building
- The AI prompt engine (exists)
- The lesson schema (exists)
- The dialogue/debrief UI (exists)
- The lesson state machine (exists)

You are building **the visual game world layer** that sits under the existing UI.

---

## 7. Key Technical Challenges

### 7.1 Flame + Flutter overlay integration
The existing dialogue cards, NarrativeBox, and Prof Penny are Flutter widgets. Flame runs as a game widget. You need to bridge them cleanly — Flame triggers events, Flutter widgets render on top.

Pattern: `GameWidget` inside a `Stack`, Flutter UI overlaid via `ValueNotifier` or `Provider`.

### 7.2 Path walking
Player walks a bezier/waypoint path automatically (not keyboard-controlled). On mobile: tap-to-walk toward interaction zone. Player stops at NPC, interaction begins.

### 7.3 Sprite sheet animation
Kenney packs come as PNG sprite sheets (e.g. 4 frames of walk cycle in a row). Flame's `SpriteAnimationComponent` handles this natively — you define frame count, frame size, FPS.

### 7.4 AI background generation
Bing Image Creator (free, DALL-E 3) generates backgrounds on demand. For the level builder, consider: generate once, save to `assets/images/`, reference by ID. Do not regenerate every run.

### 7.5 Rive (Prof Penny)
Rive is a browser design tool (rive.app). Someone needs to build the rig. The code integration in Flutter is straightforward — `RiveAnimation.asset()` + state machine controller. The bottleneck is the art, not the code.

---

## 8. Immediate First Task

Build a proof-of-concept Flame scene for Lesson 1, Path: "The Orange Quest Market":

1. Load the AI-generated market background (already in `assets/images/`)
2. Place a Kenney character sprite at 3 positions on a horizontal path
3. Player sprite walks left-to-right along the path (auto-walk, no input needed yet)
4. When player reaches NPC position, stop and fire a Flutter overlay callback
5. Overlay shows existing `DialogueScreen` widget

This is the skeleton. Everything else builds on top.

---

## 9. Dev Environment Setup

```powershell
# Flutter 3.44.2 installed at C:\flutter
# All flutter commands must run from C:\Projects\Coin-Grow\mobile\

Set-Location "C:\Projects\Coin-Grow\mobile"
C:\flutter\bin\flutter.bat pub get
C:\flutter\bin\flutter.bat run -d windows    # Windows Desktop for dev
C:\flutter\bin\flutter.bat analyze           # Must return "No issues found"
```

Android setup: Android Studio at `C:\Program Files\Android\Android Studio`. SDK Command-line tools needed — install via SDK Manager → SDK Tools.

---

## 10. Open Questions (Decisions Needed)

| Question | Options | Current lean |
|---|---|---|
| How does player move? | Auto-walk along path / tap to walk / D-pad | Auto-walk (cinematic feel) |
| Camera? | Fixed screen / follow player / pan to NPC | Pan to NPC on approach |
| NPC idle animation? | Simple bounce / Kenney sprite loop / Rive | Kenney sprite loop |
| How does level builder receive input? | Streamlit UI / CLI / hardcoded per lesson | Streamlit (already exists for prompt UI) |
| Rive for Penny? | Build rig now / placeholder sprite / later | Later — sprite placeholder first |
| Multilingual TTS? | flutter_tts (Android/iOS only) / pre-recorded / none | flutter_tts on mobile |

---

## 11. Resources

- Flame docs: `docs.flame-engine.org`
- Kenney assets: `kenney.nl/assets` (search: Tiny RPG, RPG Urban)
- Rive community: `rive.app/community` (search: owl)
- Bing Image Creator: `bing.com/images/create` (background generation)
- Lesson JSON schema: see `mobile/lib/models/lesson.dart`
- App theme / colors: see `mobile/lib/theme/app_theme.dart`

---

## 12. The Meta-Game Layer

Three new aspects sit **above** the Flame game world. None change the level builder core pipeline — they are additional keys Claude generates alongside `game_world`.

### 12.1 Monitor (Teacher / Parent Dashboard)

Teachers and parents observe a student's session in real-time, inject questions at key moments, and override which path plays next.

**Builder output (added to lesson JSON):**
```json
"monitor": {
  "checkpoints": [
    {
      "after_npc": "baker",
      "teacher_can_pause": true,
      "suggested_question": "Why didn't Eli accept the first offer?"
    }
  ],
  "teacher_controls": ["pause", "override_next_path", "inject_hint"]
}
```

**Backend:** Firebase Realtime Database. Game emits checkpoint events on NPC interaction. Teacher web app (or Flutter admin screen) subscribes and renders a class map — who's at which NPC, who's stuck.

**Build order:**
1. Read-only dashboard (ship first) — teacher sees real-time progress
2. Pause / resume control
3. Override path (redirect student to a specific scene)
4. Inject hint (push a message into the student's running game)

### 12.2 Competition (Leaderboard)

Classrooms compete against each other. Schools against schools. Age groups against age groups.

**Builder output:**
```json
"competition": {
  "leaderboard_key": "lesson_01_barter_v1",
  "tiers": ["classroom", "school", "country"],
  "metric": "score_plus_time",
  "event_window": null
}
```

**Backend:** Firebase Realtime Database. One collection per `leaderboard_key`. Score submitted on lesson completion — a single write. Leaderboard queries sort by `metric`.

`event_window` is `null` for always-on lessons. For timed competitions set an ISO date range (e.g. `"2026-10-01/2026-10-07"`).

### 12.3 Events + Social Sharing

**Events** are time-gated lessons with a seasonal banner and bonus multiplier:
```json
"event": {
  "id": "harvest_week_2026",
  "active_from": "2026-10-01",
  "active_until": "2026-10-07",
  "bonus_multiplier": 1.5,
  "banner": "🎃 Harvest Week — double points!"
}
```
No extra backend. The app reads dates on lesson load. `event` is `null` when no event is active.

**Social sharing** — builder generates the template, Flutter renders to image, OS share sheet distributes:
```json
"social": {
  "result_card_bg": "market_sunset",
  "share_text": "I bartered my way to {score}/100 in The Orange Quest! 🍊 #CoinGrow",
  "platforms": ["instagram_story", "whatsapp", "tiktok"]
}
```
Implementation: `RepaintBoundary` → `toImage()` → `share_plus` package. One widget, reused for every lesson.

### 12.4 Extended Builder Output Shape

The AI Layout Generator (Step 1) now produces all five keys:

```json
{
  "game_world": {
    "background": "medieval_market_day",
    "path": [[0.0, 0.6], [0.3, 0.58], [0.7, 0.62], [1.0, 0.6]],
    "npcs": [ ... ],
    "player_start": [0.0, 0.6],
    "interaction_radius": 0.08
  },
  "monitor": {
    "checkpoints": [ ... ],
    "teacher_controls": ["pause", "override_next_path", "inject_hint"]
  },
  "competition": {
    "leaderboard_key": "lesson_01_barter_v1",
    "tiers": ["classroom", "school", "country"],
    "metric": "score_plus_time",
    "event_window": null
  },
  "social": {
    "result_card_bg": "market_sunset",
    "share_text": "Scored {score}/100 🍊 #CoinGrow",
    "platforms": ["instagram_story", "whatsapp", "tiktok"]
  },
  "event": null
}
```

Steps 2 and 3 of the pipeline are unchanged. Asset Resolver handles `result_card_bg`. Flame Builder only reads `game_world`. The other four keys feed Firebase and the share plugin.

### 12.5 Build Priority

| Priority | Feature | Complexity | When |
|---|---|---|---|
| 1 | Score submission + classroom leaderboard | Low | Same sprint as first lesson |
| 2 | Result card + OS share sheet | Low | Same sprint as first lesson |
| 3 | Event tags + timed activation | Low | Before first classroom pilot |
| 4 | Teacher dashboard (read-only) | Medium | Before school partnership |
| 5 | Teacher inject / live override | High | Post-funding milestone |

---

## 13. The Matrix System

### Concept

Before the AI generates anything, the junior developer defines the **possibility space** of a level as a matrix of dimensions. The AI then fills each active intersection with real content: dialogue, art, NPC placement, quest hooks.

This replaces the old model of hand-crafting every NPC location. The matrix is the input. The AI generates from it.

### Dimensions

A level matrix has 2–4 dimensions. Each dimension is a list of options. Their intersections are "cells" — some cells are **active** (playable content exists), others are empty.

**Barter lesson matrix:**
```
Dimension 1 — Locations:   [market_square, river_bank, apple_farm]
Dimension 2 — Items:       [oranges, fish, apples, cloth]
Dimension 3 — Characters:  [orange_seller, fisherman, farmer]
```

**Active combinations (junior picks these):**
```
market_square  × oranges × orange_seller   → MANDATORY (must visit)
river_bank     × fish    × fisherman       → OPTIONAL quest
apple_farm     × apples  × farmer          → OPTIONAL quest
```

The active combinations tell the AI exactly which NPCs exist, where on the path they stand, and what they offer. The AI writes their dialogue and motivations from the lesson concept.

### The Matrix Input JSON

```json
"matrix": {
  "dimensions": {
    "locations": [
      { "id": "market_square", "label": "Market Square", "path_x": 0.55 },
      { "id": "river_bank",    "label": "River Bank",    "path_x": 0.2  },
      { "id": "apple_farm",    "label": "Apple Farm",    "path_x": 0.85 }
    ],
    "items": ["oranges", "fish", "apples", "cloth"],
    "characters": [
      { "id": "orange_seller", "location": "market_square", "gives": "oranges", "wants": "cloth" },
      { "id": "fisherman",     "location": "river_bank",    "gives": "fish",    "wants": "apples" },
      { "id": "farmer",        "location": "apple_farm",    "gives": "apples",  "wants": "fish" }
    ]
  },
  "active_combinations": [
    { "location": "market_square", "character": "orange_seller", "role": "mandatory" },
    { "location": "river_bank",    "character": "fisherman",     "role": "optional", "quest_id": "explore_river" },
    { "location": "apple_farm",    "character": "farmer",        "role": "optional", "quest_id": "visit_farm" }
  ]
}
```

### How Flame Uses the Matrix

Each active combination becomes a **zone** on the path:
- `path_x` places the NPC at the right position on the Flame world coordinate system
- Mandatory combinations are on the direct path — player must visit them
- Optional combinations are placed as accessible detours — player chooses to explore

The matrix replaces the hand-coded NPC position list from Section 6. The junior no longer places NPCs manually — they define the matrix, and the AI generates the positions.

### Matrix Varies Per Level

- Lesson 1 (Barter): location × item × character (3D)
- Lesson 2 (Money): time_period × item × community_trust (3D) — shows how barter → coins
- Lesson 3 (Saving): season × spending_choice × consequence (3D)
- Lesson 5 (Investment): asset_type × risk × time_horizon (3D)

The junior chooses which dimensions make sense for the concept. The builder generates from whatever matrix is provided.

---

## 14. Quest System

### Mandatory vs Optional

Every level has two quest tiers:

**Mandatory quests** (1–3 per level):
- Required for lesson completion
- Drive the main story
- Always reward points
- Unlocked by default at level start

**Optional quests** (0–5 per level):
- Never required — player can skip
- Reward bonus points (counted in leaderboard)
- Award badges on completion
- May unlock hidden dialogue or a bonus scene

### Quest JSON Schema

```json
"quests": {
  "mandatory": [
    {
      "id": "get_orange",
      "description": "Trade for an orange from the market seller",
      "target_npc": "orange_seller",
      "completion_trigger": "item_received",
      "points": 100
    }
  ],
  "optional": [
    {
      "id": "explore_river",
      "description": "Discover the fisherman at the river",
      "target_npc": "fisherman",
      "completion_trigger": "dialogue_complete",
      "points": 25,
      "badge_id": "river_explorer",
      "unlocks_scene": "fisherman_backstory"
    },
    {
      "id": "barter_chain",
      "description": "Complete a 3-step barter chain",
      "completion_trigger": "chain_complete",
      "points": 50,
      "badge_id": "chain_trader"
    }
  ]
}
```

### Quest UX in Flame

- Mandatory quests: shown in HUD as active objectives
- Optional quests: discovered when player approaches the NPC zone (not pre-announced — exploration reward)
- Quest completion triggers badge notification + score update
- On lesson end: quest summary screen shows all attempted quests, badges earned, points breakdown

---

## 15. Badges + Trophies

### Two Levels of Achievement

**Badges** — granular, per-action achievements. Accumulate on the player's profile across all lessons.

**Trophies** — per-level completion tier. Awarded once per lesson based on total performance.

### Badge Types

| Badge | Icon | Condition |
|---|---|---|
| Explorer | 🗺️ | Visited all NPCs in the level |
| Speed Runner | ⚡ | Completed mandatory quest in under 2 minutes |
| Perfect | ✨ | Zero wrong-path choices in the lesson |
| Chain Trader | 🔗 | Completed a 3-step barter chain |
| Completionist | 🏅 | Finished every optional quest |
| Social | 📱 | Shared a result card |
| Streak | 🔥 | Played 3 days in a row |

### Trophy Tiers (per level)

| Trophy | Condition |
|---|---|
| 🥉 Bronze | Completed all mandatory quests |
| 🥈 Silver | Bronze + at least 1 optional quest |
| 🥇 Gold | Silver + no wrong-path choices |
| 💎 Diamond | Gold + speed run (under time threshold) |

### Badge + Trophy JSON

```json
"achievements": {
  "trophies": [
    { "id": "bronze", "tier": "bronze", "condition": "mandatory_complete",     "icon": "🥉" },
    { "id": "silver", "tier": "silver", "condition": "bronze + 1_optional",    "icon": "🥈" },
    { "id": "gold",   "tier": "gold",   "condition": "silver + no_wrong_path", "icon": "🥇" },
    { "id": "diamond","tier": "diamond","condition": "gold + speed_run",        "icon": "💎" }
  ],
  "badges": [
    { "id": "river_explorer", "name": "River Explorer", "icon": "🗺️", "condition": "optional:explore_river" },
    { "id": "chain_trader",   "name": "Chain Trader",   "icon": "🔗", "condition": "optional:barter_chain" },
    { "id": "speed_runner",   "name": "Speed Runner",   "icon": "⚡", "condition": "time_under:120s" },
    { "id": "perfect",        "name": "Perfect",         "icon": "✨", "condition": "no_wrong_path" }
  ]
}
```

### Where Badges + Trophies Appear

- **Result card** (shareable image) — top trophy + earned badges shown
- **Player profile** — all badges collected across all lessons
- **Leaderboard** — trophy tier shown alongside score
- **Home screen** — lesson card shows player's best trophy for that lesson
